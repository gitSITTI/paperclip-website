# Agent Work Ticket: paperclip-website

Status: inventory_only
Owner: Luna maintainer queue; GPT-5.6 Sol orchestrator
Working area: Inventory-only until an explicit ticket assigns a bounded area
Approval: required before mutation, cleanup, delete, move, merge, deploy, or secret change

## Collision Rules

- Read this file and the root AGENT_WORK_REGISTRY.md before starting.
- Claim a disjoint path and record the claim in the task ticket before editing.
- Preserve unrelated dirty work. Do not reset, clean, or overwrite another agent's files.
- Use read-only inventory first. A human approval releases only the exact scope named in the approval queue.
- Producer output requires a separate QA/validator pass before the task is marked done.

## Current Assignment

No mutation assignment is active for this repo. Luna may inventory status, branches, ownership markers, and unfinished tickets.

## Handoff

Record task ID, agent/model, paths, start time, expected finish, and validation artifact in the root registry and this file before concurrent work.

